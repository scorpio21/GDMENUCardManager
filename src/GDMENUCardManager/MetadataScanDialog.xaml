<Window x:Class="GDMENUCardManager.MetadataScanDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{DynamicResource StringMetadataScanTitle}" SizeToContent="Height" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message -->
        <StackPanel Grid.Row="0">
            <TextBlock TextWrapping="Wrap" Margin="0 0 0 15" Text="{DynamicResource StringMetadataScanDesc1}" />

            <TextBlock TextWrapping="Wrap" Margin="0 0 0 10" Text="{DynamicResource StringMetadataScanDesc2}" />

            <StackPanel Margin="20 0 0 15">
                <TextBlock Text="{DynamicResource StringMetadataScanBullet1}"/>
                <TextBlock Text="{DynamicResource StringMetadataScanBullet2}"/>
                <TextBlock Text="{DynamicResource StringMetadataScanBullet3}"/>
            </StackPanel>

            <TextBlock TextWrapping="Wrap" Text="{DynamicResource StringMetadataScanDesc3}" />
        </StackPanel>

        <!-- Game count -->
        <TextBlock Grid.Row="1" Margin="0 10 0 20">
            <Run Text="{DynamicResource StringMetadataScanCount}"/><Run Text=" "/>
            <Run Text="{Binding GameCount}"/>
        </TextBlock>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource StringQuit}" Click="QuitButton_Click" Width="90" Height="28" Margin="0 0 10 0"/>
            <Button Content="{DynamicResource StringStartScan}" Click="StartScanButton_Click" Width="90" Height="28" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
